# Git 提交和推送指南

## 📋 快速步骤

### 步骤 1: 检查当前状态

```bash
git status
```

这会显示：
- 哪些文件被修改了
- 哪些文件是新添加的
- 哪些文件已暂存准备提交

### 步骤 2: 添加所有更改

```bash
git add .
```

这会将所有更改添加到暂存区（准备提交）。

**或者只添加特定文件：**
```bash
git add 文件名
```

### 步骤 3: 提交更改

```bash
git commit -m "你的提交信息"
```

**好的提交信息示例：**
```bash
git commit -m "Fix build errors and add deployment configuration"
git commit -m "Complete full stack implementation with auth and reviews"
git commit -m "Fix ESLint errors and TypeScript type issues"
```

### 步骤 4: 推送到 GitHub

```bash
git push origin main
```

如果你的默认分支是 `master`，使用：
```bash
git push origin master
```

---

## 🚀 完整命令序列

### 一次性执行所有步骤：

```bash
# 1. 检查状态
git status

# 2. 添加所有更改
git add .

# 3. 提交
git commit -m "Fix build errors and complete deployment setup"

# 4. 推送
git push origin main
```

---

## 📝 详细说明

### 1. git add

**作用**: 将文件添加到暂存区（staging area）

**常用选项**:
- `git add .` - 添加所有更改的文件
- `git add 文件名` - 添加特定文件
- `git add -A` - 添加所有更改（包括删除的文件）

### 2. git commit

**作用**: 创建一个提交，保存当前的更改

**格式**:
```bash
git commit -m "提交信息"
```

**提交信息最佳实践**:
- 使用英文（推荐）或中文
- 简洁明了，描述做了什么
- 使用动词开头（Fix, Add, Update, Remove 等）

**示例**:
```bash
git commit -m "Fix ESLint errors in submit page"
git commit -m "Add Google OAuth configuration guide"
git commit -m "Update deployment documentation"
```

### 3. git push

**作用**: 将本地提交推送到远程仓库（GitHub）

**格式**:
```bash
git push origin 分支名
```

**常用分支名**:
- `main` - 主分支（GitHub 默认）
- `master` - 旧的主分支名称
- `develop` - 开发分支

---

## 🔍 检查当前状态

运行以下命令查看你的 Git 状态：

```bash
# 查看当前状态
git status

# 查看远程仓库
git remote -v

# 查看当前分支
git branch

# 查看提交历史
git log --oneline -5
```

---

## ⚠️ 常见问题

### 问题 1: "nothing to commit"

**原因**: 没有更改需要提交

**解决**: 
- 确认你确实修改了文件
- 检查 `git status` 的输出

### 问题 2: "fatal: not a git repository"

**原因**: 当前目录不是 Git 仓库

**解决**:
```bash
# 初始化 Git 仓库
git init

# 添加远程仓库
git remote add origin https://github.com/你的用户名/仓库名.git
```

### 问题 3: "failed to push"

**原因**: 可能是权限问题或远程仓库不存在

**解决**:
- 检查远程仓库 URL：`git remote -v`
- 确认你有推送权限
- 确认远程仓库存在

### 问题 4: "branch is ahead"

**原因**: 本地有提交但未推送

**解决**: 
```bash
git push origin main
```

### 问题 5: "conflicts"

**原因**: 远程有新的提交，与本地冲突

**解决**:
```bash
# 先拉取远程更改
git pull origin main

# 解决冲突后
git add .
git commit -m "Resolve conflicts"
git push origin main
```

---

## 🎯 推荐工作流程

### 日常开发流程：

1. **修改代码**
2. **检查状态**: `git status`
3. **添加更改**: `git add .`
4. **提交**: `git commit -m "描述"`
5. **推送**: `git push origin main`

### 部署前流程：

1. **确保代码可以构建**: `npm run build`
2. **检查状态**: `git status`
3. **添加所有更改**: `git add .`
4. **提交**: `git commit -m "Ready for deployment"`
5. **推送**: `git push origin main`
6. **在 Vercel 中部署**

---

## 📚 有用的 Git 命令

```bash
# 查看更改内容
git diff

# 查看已暂存的更改
git diff --staged

# 撤销未暂存的更改
git checkout -- 文件名

# 撤销已暂存但未提交的更改
git reset HEAD 文件名

# 查看提交历史
git log

# 查看简洁的提交历史
git log --oneline

# 查看远程仓库信息
git remote -v

# 添加远程仓库
git remote add origin https://github.com/用户名/仓库名.git

# 查看当前分支
git branch

# 创建新分支
git checkout -b 新分支名

# 切换分支
git checkout 分支名
```

---

## ✅ 检查清单

提交前确认：

- [ ] 代码可以正常构建（`npm run build`）
- [ ] 没有明显的错误
- [ ] 提交信息清晰明了
- [ ] 已添加所有需要的文件
- [ ] 已排除敏感文件（.env 等）

---

## 🎉 完成！

提交和推送完成后：

1. ✅ 访问你的 GitHub 仓库
2. ✅ 确认更改已显示
3. ✅ 在 Vercel 中触发部署（如果已连接）

需要我帮你执行这些命令吗？

