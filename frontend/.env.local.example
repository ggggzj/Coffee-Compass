# Copy to .env.local and fill in your Mapbox public token.
# .env.local is gitignored — your token is never committed.
NEXT_PUBLIC_API_BASE=http://localhost:8000
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_public_mapbox_token_here
